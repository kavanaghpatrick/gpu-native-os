[package]
name = "gpu_dsl_macro"
version = "0.1.0"
edition = "2021"
description = "Proc macro for GPU bytecode DSL - THE GPU IS THE COMPUTER"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "parsing", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"
